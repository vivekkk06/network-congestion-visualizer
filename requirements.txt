dash==2.17.1
plotly==5.22.0
pandas==2.2.2
numpy==1.26.4
# Network Congestion Visualizer — Python dependencies
# Install with:  pip install -r requirements.txt

# Web dashboard
flask>=3.0.0

# Data & benchmarking
pandas>=2.0.0
matplotlib>=3.7.0

# Simulation uses only Python stdlib (socket, threading, random, time, etc.)

# Optional — uncomment to switch to Plotly Dash
# plotly>=5.0.0
# dash>=2.0.0